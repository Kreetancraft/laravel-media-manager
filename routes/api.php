<?php

/*
|--------------------------------------------------------------------------
| Media API
|--------------------------------------------------------------------------
|
| Intentionally empty. The media library is admin-only — files are served
| (WordPress-style) by exact path through the public `assets.show` route,
| which 404s on anything but an exact folder-path + filename match. There is
| no enumerable/listing API surface.
|
*/
